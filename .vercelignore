# Vercel serves the static site from the repo root and runs /api/* as one
# function. Everything below is neither.

# Migrations, seeding, the RLS test suite and the local http server. They run
# from your machine against the deployed database, never inside a request.
# api/_lib holds the code the function actually needs, so nothing here is
# imported at runtime.
server/

# Working files, not the website.
design/
10k-websites-skill/
CINEMATIC-DESIGN-PACKAGE.md
typee.md

# A second landing attempt that nothing links to.
landing-v2/
